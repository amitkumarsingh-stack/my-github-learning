# This file is located within the same repository
name: calling-reusable-template

on: workflow_dispatch

jobs:
  call-reusable-ci:
    uses: ./.github/workflows/reusable.yml  # Call reusable workflow from another repo
    with:
      language: 'python'  # Pass language input as Node.js
      build-tool: 'pip'  # Customize the